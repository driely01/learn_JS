
// declaration of function
// Performing a task
function greet(name, lastName) {
    console.log('Hello ' + name + ' ' + lastName);
}

// Calculating a value
function square(number) {
    return number * number;
}

// call the function
greet('Driss', 'El yaagoubi');

let number = square(2);
console.log(number);